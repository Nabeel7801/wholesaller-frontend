import React from 'react'
import styles from "./Button.module.css"


export const MyButton = ({ text, color, bgColor, boxShadow, hover }) => {
    return (
        <button type="button" className={styles.btn}>
            {/* <p className={classes.text}>

            </p> */}
        </button>
    )

}

